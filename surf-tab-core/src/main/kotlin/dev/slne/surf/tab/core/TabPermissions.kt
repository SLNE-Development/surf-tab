package dev.slne.surf.tab.core

object TabPermissions {
    const val PREFIX = "surf.tab"
    const val PREFIX_COMMAND = "$PREFIX.command"

    const val COMMAND_TAB = "$PREFIX_COMMAND.tab"
    const val COMMAND_TAB_RELOAD = "$COMMAND_TAB.reload"
    const val COMMAND_TAB_REFRESH = "$COMMAND_TAB.refresh"

}